const router = require('express').Router()
// const Link = require('../db/link')

router.get('/links', (req, res) => {
  // jwt from req.token
  // get all links where user id matches
  // return 200 with user links
})

router.post('/links', (req, res) => {
  // jwt from req.token
  // create new link
  // return 201
})

module.exports = router
